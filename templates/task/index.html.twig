{% extends 'base.html.twig' %}

{% block title %}Hello TaskController!
{% endblock %}

{% block body %}
	{{ form_start(form) }}
	{{ form_widget(form) }}
	<button href="{{path("app_task_edit",{id:task.id})}}" type="submit" class="button button-submit">Modifier</button>
	<a href="{{path("app_task_delete", {id:task.id})}}" class="delete-task">Supprimer la tâche</a>
	{{ form_end(form) }}
{% endblock %}
